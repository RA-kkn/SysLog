# Optional native systemd drop-in. Install explicitly; no DB/schema changes.
[Service]
KillMode=mixed
TimeoutStopSec=300
# Loaded after the existing credential EnvironmentFile; contains no credentials.
EnvironmentFile=/etc/syslog-console-udp.env
